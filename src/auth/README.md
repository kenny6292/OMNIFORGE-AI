# Authentication

OMNIFORGE uses Supabase Auth through the browser-safe publishable key.

Configure:
- VITE_SUPABASE_URL
- VITE_SUPABASE_PUBLISHABLE_KEY

The UI supports email/password sign-up and sign-in. No service-role or secret key belongs in the frontend.
